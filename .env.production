#### NOTICE: `.env.production` deprecated
# This file intentionally left minimal to avoid duplicate / leaked credentials.
# Use CI or hosting provider environment variables instead.
# Example variables to set in deployment environment (DO NOT commit real keys):
# VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
# VITE_CUSTOM_DOMAIN=true   # if building for custom root domain
# VITE_BASE=/               # optional explicit override

# (All previous secrets removed.)
